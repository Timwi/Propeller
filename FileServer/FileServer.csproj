<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Configurations>Debug-Nuget;Debug-locallibs;Release</Configurations>
    <Version>2.0.0</Version>
    <Authors>Timwi</Authors>
    <Description>A simple Propeller module that just serves files (and directory listings) from the local file system.</Description>
    <RepositoryUrl>https://github.com/Timwi/Propeller</RepositoryUrl>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\API\PropellerApi.csproj" />
  </ItemGroup>

</Project>